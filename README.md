# 헨니 체크 (Henny Check)

떨어져 사는 아이들이 **평일에 해야 할 일을 스스로 체크**하고,
부모가 **한눈에 전체 현황을 보는** 가벼운 안드로이드 앱입니다.

앱은 하나지만 첫 실행 때 역할을 고릅니다.

| 역할 | 화면 |
|---|---|
| **아이 폰** | 오늘 할 일 목록 하나. 켜기 → 누르기 → 끝. |
| **부모 폰** | 오늘 현황 / 통계 / 할 일 관리 / 설정 4개 탭. |

---

## 1. APK 받기

이 저장소는 코드만 들어 있고, APK는 GitHub Actions가 만들어 줍니다.

1. GitHub 저장소 → **Actions** 탭 → **Build APK** 워크플로
2. 가장 최근 성공한 실행을 클릭
3. 아래쪽 **Artifacts → henny-apk** 를 내려받기 (zip 안에 `henny-날짜-커밋.apk`)
4. 그 APK 파일을 아이 폰과 부모 폰에 각각 보내서 설치

설치할 때 "이 출처의 앱 설치 허용"을 한 번 켜 줘야 합니다
(설정 → 앱 → 특별한 앱 접근 → 알 수 없는 앱 설치).

> 커밋을 푸시할 때마다 자동으로 새 APK가 만들어집니다.
> 같은 서명키를 쓰므로 기존 앱 위에 덮어 설치하면 데이터가 그대로 유지됩니다.

---

## 2. 처음 설정하기 (5분)

### 부모 폰

1. 앱 실행 → **부모 폰이에요**
2. 아이를 한 명씩 추가 (예: 형, 동생)
3. 저장소 선택 화면에서 **JSONBin Master Key** 입력 → **저장 공간 만들고 시작**
   - 키 만드는 법은 아래 3번 참고
   - 지금 건너뛰고 나중에 설정 탭에서 해도 됩니다
4. **설정 탭 → 아이 → 연결 코드** 를 눌러 아이별 코드를 카톡 등으로 보냅니다

### 아이 폰

1. 앱 실행 → **아이 폰이에요**
2. 부모가 보내준 코드(`HENNY1:...`)를 붙여넣고 **연결하기**
3. 알림 권한 허용 → 끝

그다음부터 아이는 앱을 켜면 **오늘 목록**만 봅니다. 항목을 한 번 누르면 완료입니다.

---

## 3. 데이터 저장소 고르기

서버를 따로 두지 않고, 작은 JSON 문서를 주고받는 방식입니다.

- **계획 문서 1개** — 부모가 쓰고 아이들이 읽음 (아이 목록, 반복 할 일, 미션, 알림 시각)
- **기록 문서 (아이당 1개)** — 그 아이가 쓰고 부모가 읽음 (날짜별 체크 기록)

쓰는 쪽과 읽는 쪽이 문서별로 나뉘어 있어 서로 덮어쓸 일이 없습니다.

### 요청하신 dpaste / Pastebin 검토 결과

| 서비스 | 결론 |
|---|---|
| **dpaste** | ❌ 만든 붙여넣기를 **수정할 수 없습니다.** 체크할 때마다 새 URL이 생기고, 상대는 어느 게 최신인지 알 방법이 없습니다. 만료도 있습니다. |
| **Pastebin** | ⚠️ 수정 = 삭제 후 재생성이라 URL이 바뀝니다. 개발자 키 + 사용자 키가 따로 필요하고 무료 API 호출 한도가 빡빡합니다. |

두 서비스 모두 "한 번 쓰고 마는 붙여넣기"용이라 계속 갱신되는 체크 기록에는 맞지 않습니다.
그래서 **성격은 같으면서 문서 수정(PUT)이 되는** 곳을 쓰도록 만들었습니다.

### 지원하는 저장소

**① JSONBin.io (권장, 기본값)**

1. https://jsonbin.io 무료 가입
2. API Keys 메뉴에서 **X-MASTER-KEY** 복사
3. 부모 폰 설정에 붙여넣고 **저장 공간 만들기** → 문서가 자동 생성됩니다

> ⚠️ 연결 코드 안에는 이 Master Key가 들어갑니다. 즉 **아이 폰에도 같은 키가 저장됩니다.**
> 키가 있으면 그 계정의 다른 문서도 건드릴 수 있으니, 다른 용도로 쓰는 계정 말고
> **이 앱 전용 JSONBin 계정**을 하나 새로 만들어 쓰시길 권합니다.

무료 한도는 월 10,000회 호출입니다. 이 앱은 업로드를 12초간 모았다가 한 번에 보내고,
앱을 열 때와 알람이 울릴 때만 동기화하므로 **아이 2명 + 부모 1명 기준 월 2,000~3,000회** 정도를 씁니다.

**② 직접 주소 (가입/키 없음)**

`GET`으로 읽고 `PUT`(또는 `POST`)으로 덮어쓸 수 있는 JSON 주소면 무엇이든 됩니다.

- [jsonblob.com](https://jsonblob.com) — 가입 없이 만들고 `https://jsonblob.com/api/jsonBlob/{id}` 사용
- [npoint.io](https://npoint.io) — 웹에서 만들고 `https://api.npoint.io/{id}` 사용

설정 탭에서 계획 문서 주소와 아이별 기록 주소를 직접 넣습니다.
**인증이 없으므로 주소를 아는 사람은 누구나 읽고 쓸 수 있습니다.** 편한 대신 그만큼 느슨합니다.

**③ 안 씀**

인터넷을 아예 쓰지 않고 그 폰에만 저장합니다. 아이 혼자 쓰는 용도로는 이것만으로도 충분합니다.

---

## 4. 알림이 동작하는 방식

아이가 앱을 아예 안 여는 게 가장 큰 문제라, 알림을 세 종류로 나눴습니다.

1. **하루 점검 알림** — 부모가 시각을 정합니다. 기본값은
   - 아침 7:30 "오늘 할 일을 확인해요" (남은 게 없어도 알림)
   - 오후 4:00 "지금 하나만 시작해볼까?" (남은 게 있을 때만)
   - 저녁 8:00 "자기 전 마지막 점검!" (남은 게 있을 때만)
2. **마감 알림** — 할 일에 마감 시각을 넣으면 기본 1시간 전에 (30분/1시간/2시간 선택 가능)
3. **부모 요약 알림** — 저녁 8:30에 "형 4/5  동생 3/3" 형태로 하루 결과를 부모 폰에 한 번

알람은 "다음에 울릴 하나"만 예약해 두고, 울릴 때마다 그다음을 다시 예약합니다.
재부팅·앱 업데이트·시간대 변경 뒤에도 자동으로 다시 걸립니다.

### 알림이 안 울린다면

- 설정 탭 → **알림 켜기** 로 권한 확인
- 설정 탭 → **알람 권한 열기** 에서 '알람 및 리마인더' 허용 (안드로이드 12 이상)
- 배터리 최적화에서 이 앱을 **제외**로 바꿔 주세요.
  삼성/샤오미 계열은 절전 기능이 알람을 미루는 경우가 많습니다.

---

## 5. 매일 쓰는 법

**반복 할 일** — 부모 폰 → 할 일 탭 → 아이 선택 → 할 일 추가.
요일(기본 평일)과 마감 시각을 정합니다. 예: 수학 문제집 2장, 독해 문제집 2장, 바이올린 연습.
3~5개 정도가 적당합니다. 잠시 쉬게 하려면 지우지 말고 **사용 중** 스위치만 끄면 통계가 보존됩니다.

**오늘만 주는 미션** — 부모 폰 → 오늘 탭 → 아이 카드의 **오늘 미션 주기**.
"오늘은 가방 정리하는 날" 같은 걸 넣으면 아이 목록 맨 위에 **오늘의 미션** 배지로 뜹니다.

**통계** — 부모 폰 통계 탭에서 주간 막대그래프와 월간 달력, 달성률·해낸 일 개수·완벽한 날 수를 봅니다.
아이도 자기 화면 아래에서 이번 주 기록과 연속 달성 일수를 봅니다.

> 아이가 앱을 한 번도 안 연 날도 "해야 했던 개수"를 통계 분모에 넣습니다.
> 안 한 날이 안 한 날로 보이게 하려는 의도입니다.

---

## 6. 서명키에 대한 안내

`keystore/henny-family.jks` (비밀번호 `hennyfamily`)가 저장소에 들어 있습니다.
CI가 매번 같은 키로 서명해야 **덮어 설치할 때 데이터가 유지**되기 때문입니다.
가족끼리 사이드로딩하는 용도라 이렇게 두었고, Play 스토어에 올릴 계획이라면
직접 만든 키로 바꾸고 GitHub 저장소 Secrets에 아래를 넣으면 그쪽이 우선 적용됩니다.

- `KEYSTORE_BASE64` — 키스토어 파일을 base64로 인코딩한 값
- `KEYSTORE_STORE_PASSWORD`, `KEYSTORE_KEY_ALIAS`, `KEYSTORE_KEY_PASSWORD`

---

## 7. 기술 메모

- Kotlin + Jetpack Compose (Material 3), minSdk 26(안드로이드 8.0), targetSdk 35
- 데이터베이스 없음 — 작은 JSON 파일 3개로 저장하고 그대로 원격에 올립니다
- 네트워크 라이브러리 없음 — `HttpURLConnection` 하나
- WorkManager 없음 — 앱을 열 때 / 알람이 울릴 때 / 체크 후 12초 뒤에만 동기화
- R8 축소 + 리소스 축소 적용

로컬 빌드:

```bash
./gradlew assembleRelease   # app/build/outputs/apk/release/app-release.apk
```

Android SDK가 설치되어 있고 `local.properties`에 `sdk.dir`이 잡혀 있어야 합니다.

---

## 파일 구조

```
app/src/main/java/com/henny/checklist/
  MainActivity.kt        앱 진입점, 알림 권한, 열 때마다 동기화
  data/
    Models.kt            Plan / Progress 등 저장 구조
    LocalStore.kt        JSON 파일 읽고 쓰기
    RemoteStore.kt       JSONBin / 직접 주소 백엔드
    Repository.kt        단일 상태 보관소, 통계, 동기화, 연결 코드
  notify/
    AlarmScheduler.kt    "다음 하나"만 예약하는 알람 계산
    AlarmReceiver.kt     알람이 울렸을 때
    BootReceiver.kt      재부팅 후 재예약
    Notifications.kt     알림 채널과 표시
  ui/                    Compose 화면들
```
