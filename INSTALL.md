# 작업자 기기에 어떻게 설치할까

멀리 있는 작업자 기기에 앱을 넣는 방법은 크게 두 가지입니다.

1. **APK 직접 설치(사이드로딩)** — 무료, 오늘 당장 가능. 대신 폰에서 권한 스위치를 한 번 켜야 함
2. **앱 스토어를 통한 설치** — 권한 조작이 전혀 없음. 대신 준비가 필요

---

## 1. 사이드로딩의 권한이 실제로 어떤 것인가

흔한 오해부터 정리합니다.

> **"알 수 없는 앱 설치"는 폰 전체를 열어두는 스위치가 아닙니다.**
> 안드로이드 8.0(2017)부터 이 권한은 **설치를 실행하는 앱 하나에만** 붙습니다.
> 예를 들어 "내 파일" 앱에만 허용하면, 그 앱으로 실행한 설치만 가능해집니다.
> 크롬, 카톡, 드라이브는 각각 따로 허용해야 하고, 허용하지 않은 앱은 앱을 설치하지 못합니다.

그리고 **설치가 끝나면 바로 꺼도 됩니다.** 이 권한은 설치하는 그 순간에만 쓰이고,
이미 설치된 앱의 동작과는 아무 관련이 없습니다. 껐다고 앱이 멈추거나 지워지지 않습니다.

그래서 실제 위험은 "권한이 켜져 있는 동안, 그 앱을 통해 받은 다른 설치 파일을
사용자가 직접 눌러 설치하는 경우"로 한정됩니다. 작업자가 어디선가 받은 APK를 스스로
설치하는 상황이 걱정된다면, 아래처럼 **설치 직후 끄면** 그 창이 닫힙니다.

### 켜기 → 설치 → 끄기

기종마다 메뉴 이름이 조금씩 다릅니다.

| 기종 | 경로 |
|---|---|
| 삼성 (One UI) | 설정 → 앱 → 우측 상단 **⋮** → 특별한 접근 권한 → 알 수 없는 앱 설치 |
| 픽셀 / 순정 | 설정 → 앱 → 특수 앱 액세스 → 알 수 없는 앱 설치 |
| 샤오미 | 설정 → 개인정보 보호 → 특수 권한 → 알 수 없는 앱 설치 |

순서:

1. APK 파일을 작업자 기기로 보냅니다 (카톡, 드라이브, 메일 등)
2. 파일을 누르면 "이 출처의 앱을 설치할 수 없습니다" 안내가 뜨고 **설정으로 가는 버튼**이 같이 나옵니다
3. 그 버튼을 눌러 나오는 화면에서 **"이 출처 허용"** 을 켭니다
4. 뒤로 가서 설치합니다
5. **설치가 끝나면 3번 화면으로 다시 가서 스위치를 끕니다**

원격으로 시킬 때는 3~5번을 그대로 읽어주면 됩니다. 통화하면서 화면 공유를 쓰면 더 쉽습니다.

> 다만 이 과정을 **앱을 고칠 때마다** 반복해야 한다는 게 사이드로딩의 단점입니다.
> 자동 업데이트가 없기 때문입니다.

### 그런데 5번을 아무도 안 한다

이게 사이드로딩의 진짜 문제입니다. 위험한 건 권한을 켜는 순간이 아니라, **켠 뒤에 아무도
다시 끄지 않는다**는 점입니다. 설치가 끝나면 앱이 잘 돌아가니까 5번을 할 이유가 체감되지
않고, 그대로 몇 달이 지나갑니다. 멀리 있으면 확인할 방법도 없습니다.

**앱으로 해결할 수 없습니다.** 안드로이드에는 "다른 앱이 설치 권한을 갖고 있는지" 조회하는
공개 API가 없습니다. `canRequestPackageInstalls()` 는 자기 자신에 대해서만 답합니다.
그래서 헨니 체크가 "아직 안 끄셨어요"라고 알려주는 기능조차 만들 수 없습니다.

줄일 수 있는 방법은 이 정도뿐입니다.

- 권한을 **"내 파일"에만** 주세요. 크롬이나 카톡에 주면 웹에서 받은 아무 APK나 바로 설치할 수
  있게 됩니다. 파일 관리자만 허용하면 누군가 일부러 APK를 저장하고 파일 앱에서 열어야 합니다.
- **작업자 기기에는 사이드로딩하지 마세요.** 지금 당장 써보고 싶다면 본인 폰에만 넣고
  (본인은 끄는 걸 기억하실 테니), 나머지 폰은 스토어 준비가 끝난 뒤에 넣는 편이 낫습니다.
  스크린샷도 본인 폰에서 찍으면 됩니다.

**켤 권한이 없으면 끌 것도 없습니다.** 이게 스토어로 가는 진짜 이유입니다.

---

## 2. 스토어로 설치하기 — 권한 조작이 아예 없음

스토어에서 받으면 위 과정이 전부 사라집니다. 작업자는 그냥 링크를 눌러 "설치"를 누르면 되고,
그 뒤로는 **자동 업데이트**도 됩니다. 원격으로 관리하기에는 이쪽이 확실히 낫습니다.

### 선택지 비교

| | Google Play<br>내부 테스트 | Google Play<br>정식 출시 | 삼성 갤럭시 스토어 | 원스토어 |
|---|---|---|---|---|
| 비용 | US$25 (1회) | US$25 (1회) | **무료** | 무료 |
| 설치 시 권한 조작 | 없음 | 없음 | 없음 | 없음 |
| 자동 업데이트 | ○ | ○ | ○ | ○ |
| 12명 × 14일 테스트 | 불필요 | **필요**(개인 계정) | 불필요 | 불필요 |
| 심사 | 거의 없음 | 정식 심사 | 있음(비교적 가벼움) | 있음 |
| 설치 대상 | 초대한 이메일만 | 누구나 | 갤럭시 폰 | 대부분의 국내 폰 |
| 작업자가 쓰는 폰 조건 | 구글 계정 | 구글 계정 | **삼성 폰** | 원스토어 설치 |

**작업자 기기가 삼성이라면 갤럭시 스토어가 가장 현실적일 수 있습니다.** 등록비가 없고,
갤럭시 폰에는 스토어가 이미 깔려 있으며, Play의 12명·14일 요건 같은 게 없습니다.

---

## 3. 먼저 확인할 것 — $25 내기 전에

Play 내부 테스트를 고르셨다면, 돈을 내기 전에 아래 두 가지를 확인하세요.
여기서 막히면 계획이 달라집니다.

### (1) 작업자들 구글 계정이 패밀리 링크(Family Link) 관리 계정인가?

만 14세 미만 자녀 계정은 보통 패밀리 링크로 관리됩니다.
**관리 대상(supervised) 계정은 Play의 테스트 트랙(내부/비공개/공개 테스트)에 참여하지 못하는 경우가 있습니다.**
확인 방법:

1. Play 개발자 계정을 만들기 **전에**, 아무 앱이나 공개 테스트 중인 것을 골라
   작업자 계정으로 참여 링크를 열어 봅니다
2. "이 계정은 테스트에 참여할 수 없습니다" 같은 안내가 나오면 → 내부 테스트 경로는 막힙니다

막힌 경우의 대안:
- 작업자 기기를 **관리 대상이 아닌 계정**으로 Play에 로그인시킨다
- **갤럭시 스토어**로 간다 (삼성 폰이라면)
- 정식 출시로 간다 — 단 개인 계정이면 12명·14일 요건을 통과해야 함

### (2) 이미 가지고 있는 Play 개발자 계정이 있는가?

**2023년 11월 13일 이전에 만든 개인 계정**은 12명·14일 요건이 적용되지 않습니다.
예전에 만들어 둔 계정이 있다면 정식 출시까지 훨씬 수월합니다.

---

## 4. 그래서 뭘 고를까

```
작업자 기기가 삼성인가?
├─ 예 → 갤럭시 스토어 (무료, 권한 조작 없음, 12명 요건 없음)
│        └─ 잘 안 되면 ↓
└─ 아니오 ─┐
            ↓
작업자 구글 계정이 패밀리 링크 관리 계정인가?
├─ 아니오 → Play 내부 테스트  ← 대체로 가장 깔끔
└─ 예 ─┐
        ↓
   ├─ 관리 대상 아닌 계정으로 바꿀 수 있다 → Play 내부 테스트
   ├─ 2023년 11월 이전 개발자 계정이 있다 → Play 정식 출시
   └─ 둘 다 아니다 → 사이드로딩(1번) 으로 시작하고,
                     시간 될 때 12명·14일 테스트를 진행
```

**당장 오늘 쓰고 싶다면** 1번 사이드로딩으로 시작하시고,
스토어 준비는 그와 별개로 천천히 진행하시면 됩니다. 앱과 데이터는 그대로 옮겨집니다.
단, 스토어 버전은 서명 키가 달라 **한 번은 기존 앱을 지우고 새로 설치**해야 합니다.
그때 팀 저장소를 쓰고 있었다면 기록은 서버에서 다시 내려받으므로 잃지 않습니다.

---

## 4.5 우리 집 결론

확인된 조건:

- 작업자 기기: **둘 다 삼성 갤럭시**
- 작업자들 구글 계정: **패밀리 링크 관리 계정이 아닌 일반 계정**
- Play 개발자 계정: **없음**

패밀리 링크 관리 계정이 아니므로 **Play 내부 테스트 경로가 막히지 않습니다.**
갤럭시라 갤럭시 스토어도 쓸 수 있습니다. 둘 중에서는 이렇게 갈립니다.

| | Play 내부 테스트 | 갤럭시 스토어 |
|---|---|---|
| 비용 | **US$25** (최초 1회) | 무료 |
| 앱이 공개되나 | **비공개.** 초대한 이메일만 봄 | **공개 목록에 올라감** |
| 준비 기간 | 계정 신분 확인에 며칠 | 심사에 며칠 |
| 12명·14일 요건 | 없음 | 없음 |

두 방식 모두 **설치할 때 권한을 켤 일이 없습니다.** 작업자가 링크를 눌러 "설치"만 누르면 되고,
그 뒤 업데이트도 자동입니다. 끄는 걸 기억할 필요가 아예 사라집니다.

**Play 내부 테스트를 권합니다.** 둘 사이의 결정적인 차이는 비용이 아니라 **공개 여부**입니다.
이 앱에는 작업자들 이름과 생활 패턴이 들어갑니다. 갤럭시 스토어는 누구나 검색해 받을 수 있는
공개 목록에 올라가지만, Play 내부 테스트는 초대한 이메일 외에는 존재 자체를 모릅니다.
소규모 팀에는 이 차이가 $25보다 큽니다.

> 돈을 쓰기 싫으시면 갤럭시 스토어도 동작은 똑같이 합니다. 다만 스토어 등록정보에
> 작업자 이름 같은 걸 넣지 마시고, 스크린샷도 예시 이름으로 찍으세요.

다음 할 일은 [PLAY_STORE.md](PLAY_STORE.md) 1단계(업로드 키 만들기)부터입니다.

---

## 5. 참고: 왜 다른 방법은 답이 아닌가

- **Firebase App Distribution** — 무료지만 결국 APK 설치라 "알 수 없는 앱 설치" 권한이 똑같이 필요합니다. 해결이 안 됩니다.
- **Play 내부 앱 공유(Internal app sharing)** — Play 스토어 설정에서 개발자 모드를 켜야 해서 오히려 더 어렵습니다.

### 웹앱(PWA)은? — 알림은 되지만 조건이 붙습니다

여기서 **두 가지를 구분**해야 합니다. 흔히 뭉뚱그려 "PWA 알림"이라고 부르지만 성격이 다릅니다.

| | PWA 에서 되나 | 필요한 것 |
|---|---|---|
| **푸시 알림** (서버가 보내면 뜸) | ✅ **됩니다.** 안드로이드 크롬·삼성 인터넷 모두 오래전부터 지원. 앱을 닫아 놔도 옵니다 | 구독을 보관하고 **때맞춰 쏴 줄 서버** |
| **기기 스스로 예약** (오후 4시에 혼자 울림) | ❌ 안 됩니다 | — |

즉 "PWA = 푸시 알림 가능"은 맞는 말입니다. 다만 **누군가 그 시각에 보내 줘야** 뜹니다.
브라우저가 알아서 예약해 두는 기능(Notification Triggers API)은 크롬에서 실험만 하다 접었고,
Periodic Background Sync 는 브라우저가 시점을 정하는 방식이라 "오후 4시 정각"에는 못 씁니다.

지금 이 앱은 **기기 안의 AlarmManager 가 스스로 예약**해서 서버 없이 울립니다.
PWA 로 옮기면 이 부분을 **보내 주는 쪽(스케줄러)** 으로 대체해야 합니다.

**스케줄러 후보**

- **GitHub Actions 예약 워크플로** — 이미 쓰고 있어서 추가 비용이 없습니다.
  다만 예약 실행은 **정시를 보장하지 않습니다.** 몇 분에서 수십 분 늦기도 하고 건너뛰기도 합니다.
  또 저장소에 60일간 활동이 없으면 예약이 자동으로 꺼집니다.
- **Cloudflare Workers 의 Cron Trigger** — 무료 한도 안에서 훨씬 정확합니다. 계정을 하나 더 만들어야 합니다.
- **Firebase Cloud Functions** — 정확하지만 함수 사용에 결제 수단 등록(Blaze)이 필요합니다.

**정직한 비교:** 지금 방식(네이티브 정확 알람 + 절전 예외)이 웹 푸시보다 **더 잘 울립니다.**
웹 푸시는 스케줄러 → 푸시 서비스 → 브라우저 → 서비스 워커를 거치는데, 그 사이 어디서든
지연이 생길 수 있고 삼성 절전이 브라우저 백그라운드를 잡을 여지도 있습니다.
"4시쯤 울리면 된다" 면 충분하지만, "정확히 울려야 한다" 면 네이티브가 낫습니다.

**절충안: 네이티브 껍데기 + 웹 화면.** 알림 예약은 지금처럼 기기가 스스로 하고(서버 불필요,
정확도 유지), 화면과 로직만 GitHub Pages 에 올려 WebView 로 띄웁니다. 그러면 빌드가 끝나는
순간 모든 기기에 반영되고, 껍데기 APK 는 알림 규칙을 바꿀 때만 다시 깔면 됩니다.
대신 지금의 Compose 화면을 HTML/JS 로 다시 써야 해서 작업량이 큽니다.
