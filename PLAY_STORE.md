# 구글 플레이 출시 안내

이 문서는 헨니 체크를 Google Play에 올리기 위해 **직접 해야 하는 일**만 순서대로 적은 것입니다.
빌드 쪽(AAB 생성, 서명, 버전 번호)은 이미 저장소에 준비돼 있습니다.

---

> **설치 방법 자체를 아직 못 정하셨다면 [INSTALL.md](INSTALL.md) 를 먼저 보세요.**
> 갤럭시 스토어가 더 나은 경우도 있고, $25를 내기 전에 확인해야 할 것이 두 가지 있습니다.

## 0. 먼저 고를 것 — 내부 테스트인가, 정식 출시인가

소수 인원이 쓰려는 앱이라면 **내부 테스트(Internal testing)** 를 권합니다.

| | 내부 테스트 | 정식 출시(프로덕션) |
|---|---|---|
| 설치 방법 | 초대한 이메일로 Play 링크 | 누구나 검색·설치 |
| 자동 업데이트 | ○ | ○ |
| 테스터 수 | 최대 100명 | 제한 없음 |
| **개인 계정의 12명 × 14일 비공개 테스트** | **불필요** | **필요** |
| 심사 | 거의 없음, 보통 몇 분~수 시간 | 정식 심사, 며칠 |
| 스토어 등록정보 공개 | 비공개 | 공개 |

2023년 11월 이후에 만든 **개인(personal) 개발자 계정**은 프로덕션 출시 전에
**테스터 12명이 14일 연속으로 비공개 테스트에 참여**해야 합니다. 소규모 팀에는 꽤 큰 벽입니다.
(사업자 등록이 있는 **조직(organization) 계정**은 이 요건이 적용되지 않지만 D-U-N-S 번호가 필요합니다.)

> 내부 테스트만으로도 원하시던 것 — 작업자 기기에 Play로 설치, 자동 업데이트, **설치 시 권한 조작 없음**
> — 은 모두 됩니다. 아래 3~7단계는 두 경로 모두에 필요하고, 8단계만 프로덕션 전용입니다.

> ⚠️ **돈 내기 전에 확인:** 만 14세 미만 자녀의 구글 계정은 보통 패밀리 링크로 관리되는데,
> **관리 대상 계정은 Play 테스트 트랙에 참여하지 못할 수 있습니다.**
> 확인 방법과 대안은 [INSTALL.md 3절](INSTALL.md#3-먼저-확인할-것--25-내기-전에)에 있습니다.

---

## 1. 업로드 키 만들기 (필수, 제일 먼저)

### 이게 왜 필요한가

안드로이드는 **모든 앱에 전자 서명을 요구합니다.** 서명이 하는 일은 하나뿐입니다 —
"이 업데이트는 처음 그 앱을 만든 사람이 올린 게 맞다"를 증명하는 것. 이게 없으면
아무나 남의 앱인 척 업데이트를 밀어 넣을 수 있게 됩니다.

Play 에서는 열쇠가 **두 개**로 나뉩니다.

| | 누가 갖고 있나 | 하는 일 |
|---|---|---|
| **앱 서명 키** | **구글** 이 만들어 보관 | 실제로 사용자 폰에 설치될 때 검증되는 열쇠 |
| **업로드 키** | **내가** 만들어 보관 | Play 에 파일을 올릴 때 "나 맞다"를 보여주는 신분증 |

우리가 만드는 건 아래쪽, **업로드 키**입니다. Play 콘솔에 신분증을 내미는 용도라고 보면 됩니다.
중요한 진짜 열쇠는 구글이 보관하므로, 우리가 잃어버려도 앱이 망가지지 않습니다.

> **잃어버려도 복구됩니다.** Play 앱 서명을 쓰면 업로드 키는 Play 콘솔에서 재설정을
> 요청할 수 있습니다. (예전에는 키를 잃으면 앱을 영영 못 올렸는데, 그건 앱 서명 키를
> 직접 들고 있던 시절 이야기입니다.) 그래도 비밀번호 관리자에 넣어 두시는 게 편합니다.

정리하면: **한 번 만들어 GitHub Secrets 에 넣어 두면, 그 뒤로는 다시 볼 일이 없습니다.**

### 컴퓨터 없이 태블릿에서 만들기

`keytool` 이라는 명령을 한 번 실행하면 되는데, 브라우저에서 쓸 수 있는 무료 리눅스
터미널을 쓰면 태블릿에서도 됩니다. 둘 중 아무거나 고르세요.

**방법 A — 구글 Cloud Shell (권장)**

이미 구글 계정이 있으니 추가 가입이 없고, `keytool` 이 미리 깔려 있습니다.

1. 태블릿 브라우저로 <https://shell.cloud.google.com> 접속 → 구글 로그인
2. 약관에 동의하면 검은 터미널 창이 뜹니다 (결제 정보 필요 없음)
3. 아래를 **한 줄씩** 붙여넣고 실행합니다. `내가정한비밀번호` 는 원하는 값으로 바꾸세요

   ```bash
   keytool -genkeypair -v -keystore henny-upload.jks -storetype PKCS12 \
     -alias henny -keyalg RSA -keysize 2048 -validity 10950 \
     -dname "CN=Henny Check, O=Henny, C=KR" \
     -storepass '내가정한비밀번호' -keypass '내가정한비밀번호'
   ```

4. base64 문자열로 바꿔 화면에 출력합니다

   ```bash
   base64 -w0 henny-upload.jks
   ```

5. 출력된 긴 한 줄을 **전부** 복사합니다 (터미널에서 길게 눌러 전체 선택)
6. 백업용으로 파일도 받아 두세요: 터미널 오른쪽 위 **⋮ → 다운로드** → `henny-upload.jks`

**방법 B — GitHub Codespaces**

GitHub 계정만 있으면 되고, 이미 저장소가 GitHub 에 있으니 자연스럽습니다.

1. 태블릿 브라우저로 저장소 접속 → 초록색 **Code** 버튼 → **Codespaces** 탭 → **Create codespace**
2. 뜨는 터미널에서 위 3~5번을 똑같이 실행 (Java 가 미리 깔려 있습니다)
3. 다 하면 Codespace 를 **삭제**하세요 (키를 남겨두지 않기 위해)

> ⚠️ **GitHub Actions 안에서 만들면 안 됩니다.** 공개 저장소의 Actions 로그와 아티팩트는
> 누구나 볼 수 있어서 키가 그대로 새어 나갑니다.

**방법 C — 컴퓨터가 생기면**

JDK 를 깔고 위 3번 명령을 그대로 실행하면 됩니다.
Android Studio 가 있다면 `Build → Generate Signed Bundle / APK → Create new...` 로
화면에서 만들 수도 있습니다.

### GitHub Secrets 에 넣기 (태블릿 브라우저로 가능)

**바로 가는 주소:** <https://github.com/nohkwangsun/henny/settings/secrets/actions>

메뉴로 찾아가려면: 저장소 → **Settings** → 왼쪽 사이드바 **Secrets and variables** →
**Actions** → **Secrets** 탭 → 초록색 **New repository secret** 버튼

화면에서 헷갈리기 쉬운 것들을 먼저 정리합니다.

| 보이는 것 | 골라야 하나 | 왜 |
|---|---|---|
| 사이드바 **Actions** / Codespaces / Dependabot | **Actions** | 앱을 빌드하는 게 Actions 이기 때문 |
| **Secrets** 탭 / Variables 탭 | **Secrets** | 비밀번호와 키라서. Variables 는 값이 그대로 보임 |
| **Repository secrets** / Environment secrets | **Repository** | 우리는 배포 환경(Environment)을 만들지 않았음 |
| Organization secrets | 해당 없음 | 조직 계정에만 나타남. 개인 계정이면 안 보임 |

**New repository secret** 을 누르면 칸이 두 개 나옵니다. **Name** 과 **Secret**.
아래 4개를 하나씩 넣고 매번 **Add secret** 을 누릅니다. 이름은 대소문자까지 정확해야 합니다.

| Name (그대로 입력) | Secret (넣을 값) |
|---|---|
| `KEYSTORE_BASE64` | `base64 -w0` 로 출력된 긴 한 줄 전체 |
| `KEYSTORE_STORE_PASSWORD` | 키 만들 때 정한 비밀번호 |
| `KEYSTORE_KEY_ALIAS` | `henny` |
| `KEYSTORE_KEY_PASSWORD` | 키 만들 때 정한 비밀번호 (위와 같은 값) |

알아두면 좋은 것:

- **저장하면 값을 다시 볼 수 없습니다.** 덮어쓰기만 됩니다. 잘못 넣었다 싶으면
  같은 이름으로 다시 등록(Update)하면 그만입니다.
- 붙여넣을 때 **앞뒤 공백이나 따옴표가 딸려오지 않게** 하세요. 줄바꿈은 괜찮습니다.
- 4개를 다 넣으면 목록에 이름 4개가 보입니다. 값은 안 보이는 게 정상입니다.

### 제대로 들어갔는지 확인

아무 커밋이나 푸시하거나, Actions 탭 → **Build APK** → **Run workflow** 를 누릅니다.
실행이 끝나면 두 가지를 봅니다.

1. **Prepare signing key** 단계 로그에 `진짜 업로드 키로 서명합니다` 가 나오면 성공입니다.
   (`업로드 키 시크릿이 없습니다` 경고가 뜨면 이름을 잘못 넣은 것입니다.)
2. **Artifacts → henny-apk** 안에 **`.aab` 파일이 생겼으면** 끝입니다.
   이 파일을 Play 콘솔에 올립니다.

> 이전에 저장소에 들어 있던 `keystore/henny-family.jks` 는 삭제했습니다.
> 공개 저장소에 노출됐던 키라 **재사용하지 마세요.**

---

## 2. 개발자 계정

<https://play.google.com/console> 에서 등록. **US$25 최초 1회**.
개인 계정은 신분증 확인, 주소, 전화번호 확인이 필요하고 며칠 걸릴 수 있습니다.

---

## 3. 개인정보처리방침 URL 만들기

Play는 개인정보처리방침 **공개 URL**을 반드시 요구합니다. 저장소에 이미 만들어 뒀습니다.

1. `docs/privacy.html` 을 열어 **`CONTACT_EMAIL` 두 곳을 실제 이메일로 바꾸세요.**
   (이 주소는 스토어에도 공개되므로, 원하시면 이 앱 전용 주소를 따로 만드는 걸 권합니다.)
2. GitHub 저장소 → **Settings → Pages** → Source: `Deploy from a branch`,
   Branch: `main`(또는 기본 브랜치) / 폴더 `/docs` → Save
3. 몇 분 뒤 이 주소가 열립니다:

   ```
   https://nohkwangsun.github.io/henny/privacy.html
   ```

   이 URL을 Play 콘솔의 개인정보처리방침 칸에 넣습니다.

---

## 4. 스토어 등록정보에 넣을 것

준비돼 있는 것 (`store/` 폴더):

- `store/icon-512.png` — 앱 아이콘 512×512
- `store/feature-graphic-1024x500.png` — 기능 그래픽 1024×500

(다시 만들려면 `python3 tools/make_store_graphics.py`)

**직접 준비해야 하는 것: 스크린샷.**
Play는 휴대전화 스크린샷을 **최소 2장**(권장 4~8장) 요구합니다. 실제 앱 화면이어야 하므로
폰에서 직접 찍으세요. 규격은 세로 16:9~9:16, 짧은 변 320px 이상, 긴 변 3840px 이하 —
요즘 폰의 기본 스크린샷은 그대로 통과합니다. 찍으면 좋은 화면:

1. 작업자 화면 — 할 일이 몇 개 체크된 상태
2. 작업자 화면 — 전부 완료된 상태
3. 관리자 오늘 탭 — 작업자 두 명 현황
4. 관리자 통계 탭 — 주간 막대그래프
5. 관리자 할 일 관리 탭

**텍스트 항목** (초안, 그대로 쓰셔도 됩니다):

- 앱 이름 (30자): `헨니 체크: 작업 체크리스트`
- 간단한 설명 (80자):
  `작업자는 오늘 할 일을 한 번에 체크하고, 관리자는 주·월 달성률을 한눈에 봅니다.`
- 자세한 설명 (4000자):

```
떨어져 있는 사람에게 반복 작업을 맡기고, 제대로 됐는지 확인하기 위한 아주 단순한
체크리스트입니다. 계정도 로그인도 없습니다.

■ 작업자는 화면 하나만 봅니다
앱을 켜면 바로 오늘 작업이 나옵니다. 한 번 누르면 완료. 그게 전부입니다.
아래에는 이번 주 기록과 며칠 연속으로 다 해냈는지가 나와 스스로 관리하게 됩니다.

■ 관리자는 전체를 봅니다
작업자별로 오늘 몇 개를 했는지, 몇 시에 체크했는지 확인합니다.
주간 막대그래프와 월간 달력으로 달성률, 완료 건수, 전부 해낸 날을 봅니다.

■ 잊지 않게 알려줍니다
· 아침에 오늘 작업 목록을 알림으로 펼쳐 보여줍니다
· 오후와 저녁에는 아직 남은 게 있을 때만 울립니다
· 마감 시각을 정한 작업은 정해진 시간 전에 미리 알려줍니다
· 관리자 기기에는 저녁에 하루 요약이 한 번 옵니다

■ 정기 작업과 임시 작업
일일 점검표 작성, 재고 확인처럼 매번 반복하는 일은 요일을 정해 두고,
"오늘은 창고 재고 확인" 같은 하루짜리 일은 관리자가 그때그때 배정합니다.
작업자마다 다른 작업을 지정할 수 있습니다.

■ 가볍고 조용합니다
앱 크기 약 1MB. 광고 없음, 결제 없음, 회원가입 없음, 추적 없음.
기본값은 이 기기에만 저장이며, 관리자가 직접 설정한 경우에만 기기끼리 내용을 주고받습니다.
```

- 앱 카테고리: **생산성(Productivity)**
- 태그: 할 일, 체크리스트, 업무 관리, 일정

---

## 5. 앱 콘텐츠 선언 — 이 앱 기준 답안

Play 콘솔 → **정책 → 앱 콘텐츠**. 아래대로 답하면 됩니다.

### 개인정보처리방침
3단계에서 만든 URL.

### 광고
**아니요, 앱에 광고가 없습니다.** (사실입니다 — 광고 SDK가 하나도 없습니다.)

### 앱 액세스 권한
**로그인 없이 모든 기능 사용 가능.** 계정이 없습니다.
심사자를 위해 이렇게 적어 두세요:
> 첫 실행에서 "관리자 폰이에요"를 고르고 작업자를 추가하면 모든 기능을 볼 수 있습니다.
> 저장소는 "나중에 하기"를 선택하면 됩니다. 로그인 정보가 필요 없습니다.

### 콘텐츠 등급 (설문)
폭력·성적 내용·욕설·도박·약물 전부 **아니요**. 사용자 간 소통 기능 **없음**,
사용자 위치 공유 **없음**, 구매 **없음**. → 전체이용가 / 3+ 가 나옵니다.

### 타겟 층 및 콘텐츠
- 타겟 연령: **18세 이상** (일반 업무용 도구)
- 아동이 관심을 가질 만한가: **아니요**
- → 이렇게 답하면 **가족 정책(Families Policy)이 적용되지 않습니다.** 아래 7번 참고.

> ⚠️ **단, 이 답이 유효하려면 스토어 등록정보에 아동을 겨냥한 표현이 없어야 합니다.**
> 설명·스크린샷·아이콘 어디에도 "자녀", "초등학생", "숙제" 같은 말이나 아동용 그림이
> 들어가면 안 됩니다. 위 4번의 문구와 중립적인 아이콘은 이 기준에 맞춰 써 두었습니다.
> 실제로 누가 쓰는지와 무관하게, **앱이 무엇을 겨냥해 만들어지고 홍보되는가**가 기준입니다.

### 데이터 보안 (Data safety)
"앱이 필수 사용자 데이터 유형을 수집하거나 공유하나요?" → **예**
(팀 저장소를 설정하면 기기 밖으로 나가므로 "수집"에 해당합니다.)

| 데이터 유형 | 수집 | 공유 | 필수/선택 | 목적 |
|---|---|---|---|---|
| 개인 정보 → 이름 | 예 | 아니요 | **선택** | 앱 기능 |
| 앱 활동 → 기타 사용자 생성 콘텐츠<br>(할 일 제목, 체크 기록) | 예 | 아니요 | **선택** | 앱 기능 |

그 밖의 항목(위치, 금융, 연락처, 사진, 오디오, 기기 ID, 앱 성능 등)은 **전부 아니요**.

추가 질문:
- 전송 중 데이터가 암호화되나요? → **예** (모두 HTTPS)
- 사용자가 데이터 삭제를 요청할 수 있나요? → **예**
  (앱 안에 설정 → **모든 데이터 지우기** 가 있습니다)
- 데이터 수집이 선택 사항인가요? → **예** (기본값은 동기화 안 함)

> "공유(Shared)"를 아니요로 답하는 근거: 자료는 **이용자 본인이 고르고 본인 계정으로 운영하는
> 저장 서비스**로만 갑니다. 개발자나 제3자에게 넘어가지 않습니다. Play 정의상 이용자를 대신하는
> 저장 서비스로의 전송은 "공유"가 아닙니다. 개인정보처리방침에도 그렇게 적어 두었습니다.

### 정부 앱 / 금융 앱 / 뉴스 앱 / 헬스 앱
전부 **아니요**.

---

## 6. AAB 올리기

1. Actions → 최신 성공한 **Build APK** 실행 → Artifacts → `henny-apk` 다운로드
2. 압축을 풀면 `henny-v1.0.N-xxxxxxx.aab` 가 있습니다. 이걸 올립니다. (APK가 아니라 **AAB**)
3. Play 콘솔 → 앱 만들기 → 테스트 → **내부 테스트** → 새 버전 만들기 → AAB 업로드
4. **Play 앱 서명**은 자동으로 켜집니다. 구글이 실제 배포용 서명 키를 보관하고,
   우리가 만든 업로드 키는 "업로드 확인용"으로만 쓰입니다. 그대로 두시면 됩니다.
5. 테스터: 이메일 목록에 본인과 작업자들 계정을 넣고 저장 → 나오는 **참여 링크**를
   각 폰에서 열어 참여 → Play에서 설치

`henny-mapping` 아티팩트의 `mapping.txt` 를 같은 버전에 올려두면 크래시 로그를 읽을 수 있습니다.

---

## 7. 가족 정책(Families Policy)

타겟 연령을 18세 이상으로 선언하면 **적용되지 않습니다.** 6번의 조건(등록정보에 아동을
겨냥한 표현이 없을 것)만 지키면 됩니다.

다만 나중에 마음이 바뀌어 아동용으로 홍보하거나 타겟 연령에 13세 미만을 넣으면
가족 정책이 적용됩니다. 그때를 대비해 이 앱의 현재 상태를 적어둡니다. 대부분 이미 충족합니다.

| 요구사항 | 현재 상태 |
|---|---|
| 광고는 구글 인증 SDK만 | ✅ 광고 자체가 없음 |
| 외부 SDK는 아동용 승인된 것만 | ✅ 외부 SDK가 하나도 없음 |
| 광고 ID(AAID) 수집 금지 | ✅ 수집 안 함 |
| 위치·연락처·카메라 등 미요청 | ✅ 요청 안 함 |
| 결제·유도 없음 | ✅ 없음 |
| 개인정보 입력 최소화 | ✅ 표시 이름 하나뿐이고 실명이 아니어도 됨 |
| 자료가 외부 저장소로 전송됨 | ⚠️ 기본 저장소가 구글 Firebase 라 설명은 쉬움. 기본값은 동기화 안 함 |

---

## 8. 프로덕션(정식 출시)까지 가려면 — 개인 계정 추가 절차

1. 비공개 테스트(Closed testing) 트랙을 만들고 **테스터 12명 이상**을 초대
2. 12명이 **14일 연속** 옵트인 상태를 유지 (실제로 써 보게 하는 것이 취지입니다)
3. 그 뒤 **프로덕션 액세스 신청** 양식 작성 → 검토
4. 통과하면 프로덕션 트랙에 출시 가능

> 지인 12명을 14일 붙잡아 두기 어려우면 내부 테스트로 계속 쓰셔도 아무 문제 없습니다.
> 기능·자동 업데이트 모두 동일합니다.

---

## 9. 업데이트하는 법

1. 코드를 고쳐서 push
2. Actions가 자동으로 새 AAB를 만듭니다 (`versionCode`는 워크플로 실행 번호라 항상 증가)
3. Play 콘솔에서 새 버전 만들기 → AAB 업로드 → 출시

`versionName`은 `1.0.<실행번호>` 형태로 자동 생성됩니다. 직접 정하고 싶으면
`.github/workflows/android.yml` 의 `HENNY_VERSION_NAME` 을 고치세요.

---

## 9.5 저장소를 비공개로 돌리는 것에 대해

지금 `nohkwangsun/henny` 는 **공개 저장소**입니다. 소스만 있고 개인정보는 없지만,
비공개로 바꾸면 Actions 아티팩트(빌드된 APK)도 외부에서 못 받게 되어 더 깔끔합니다.

다만 **비공개로 바꾸면 GitHub Pages가 막힙니다.** 비공개 저장소의 Pages는 유료 플랜
기능이라, 3단계에서 만든 개인정보처리방침 URL이 죽습니다. Play는 이 URL이 살아 있어야 하므로
비공개로 돌리려면 방침을 다른 곳에 올려야 합니다.

- 공개 Gist 로 올리고 그 주소를 쓰기 (가장 간단)
- 방침 파일만 담은 작은 공개 저장소를 따로 만들어 Pages 켜기
- 이미 쓰는 블로그나 노션 공개 페이지에 붙여넣기

우선순위를 정하자면, **개인정보처리방침 URL이 살아 있는 게 먼저**입니다.
저장소 공개 여부는 그다음 문제입니다.

---

## 10. 기술 메모

- **패키지명 `com.henny.checklist` 는 출시 후 영원히 바꿀 수 없습니다.** 지금 바꾸려면
  `app/build.gradle.kts` 의 `applicationId`, `namespace`, 그리고 소스의 패키지 경로를 함께 바꾸세요.
- `targetSdk` / `compileSdk` = **36 (Android 16)**. Play는 매년 8월 말에 신규 앱의 최소
  타겟 API를 올립니다. 콘솔이 경고를 띄우면 그 숫자에 맞춰 올리면 됩니다.
- 릴리스 빌드는 R8 축소 + 리소스 축소가 켜져 있고, `mapping.txt` 를 아티팩트로 남깁니다.
